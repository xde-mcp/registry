services:
  registry:
    container_name: registry-integration-test
    environment:
      - MCP_REGISTRY_SEED_FROM=
      - MCP_REGISTRY_ENABLE_REGISTRY_VALIDATION=false
    healthcheck:
      test: ["CMD", "wget", "-qO-", "http://localhost:8080/v0/servers"]
      interval: 1s
      retries: 60
      timeout: 1s
  postgres:
    container_name: postgres-integration-test
    volumes: !reset []
    tmpfs:
      - /var/lib/postgresql/data:size=50m
